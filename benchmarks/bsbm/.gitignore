# BSBM toolkit + generated test-driver data — all regenerable from
# setup-bsbmtools.sh + datasets/<scale>/fetch-data.sh (deterministic generator).
bsbmtools-0.2/
bsbmtools-v0.2.zip
datasets/*/td/

# Keep reports/<release>/runs/**/*.xml — the raw driver output behind the README
# tables (the BSBM analogue of sparqloscope's engines/*.tsv), small + text.
