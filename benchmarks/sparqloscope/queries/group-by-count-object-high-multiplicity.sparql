# GROUP BY with COUNT, for object with high multiplicity
PREFIX rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#> SELECT ?object (COUNT(?subject) AS ?count) { ?subject rdf:type ?object . } GROUP BY ?object ORDER BY DESC(?count) LIMIT 10
