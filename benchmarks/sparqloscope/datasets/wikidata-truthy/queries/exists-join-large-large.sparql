# EXISTS JOIN of two large predicates
PREFIX schema: <http://schema.org/> PREFIX p: <http://www.wikidata.org/prop/> SELECT (COUNT(*) AS ?count) WHERE { ?s schema:description ?o1 FILTER EXISTS { ?s schema:name ?o2 } }
